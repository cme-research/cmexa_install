services:

  mosquitto:
    image: eclipse-mosquitto:latest
    container_name: mosquitto
    restart: unless-stopped
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - ./docker/mosquitto/bridge.conf:/mosquitto/config/mosquitto.conf:ro
      - mosquitto_data:/mosquitto/data
      - mosquitto_log:/mosquitto/log
    healthcheck:
      test: ["CMD-SHELL", "mosquitto_sub -t '$$SYS/#' -C 1 -i healthcheck -W 3 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s

  webapp:
    image: ghcr.io/cme-research/cmexa_webapp:latest
    container_name: cmexa-webapp
    restart: unless-stopped
    network_mode: host
    depends_on:
      mosquitto:
        condition: service_healthy
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro

  brickd:
    image: cmeresearch/brickd:1.0
    container_name: cmexa-brickd
    restart: unless-stopped
    network_mode: host
    privileged: true
    device_cgroup_rules:
      - 'c 189:* rmw'
    volumes:
      - /dev/bus/usb:/dev/bus/usb

  hardware:
    image: ghcr.io/cme-research/cmexa_hardware:latest
    container_name: cmexaiii-hardware
    restart: unless-stopped
    network_mode: host
    privileged: true
    ipc: host
    depends_on:
      mosquitto:
        condition: service_healthy
      brickd:
        condition: service_started
    volumes:
      - /dev/input:/dev/input
    device_cgroup_rules:
      - 'c 189:* rmw'
    group_add:
      - "${INPUT_GID}"
    environment:
      - ROBOT=cmexaiii
      - ROBOT_ENV=house

  nav:
    image: ghcr.io/cme-research/cmexa_nav:latest
    container_name: cmexaiii-nav
    restart: unless-stopped
    network_mode: host
    ipc: host
    depends_on:
      hardware:
        condition: service_started
    command: ros2 launch cmeresearch_bringup ${LAUNCH_FILE:-cmexaiii_nav_mapping.launch.py}
    volumes:
      - /robot/data/maps:/maps

volumes:
  mosquitto_data:
  mosquitto_log:
